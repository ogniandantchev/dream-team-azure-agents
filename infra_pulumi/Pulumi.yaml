name: dream-team-agents-pulumi
runtime: python
description: Infrastructure for Dream Team Azure Agents using Microsoft Agent Framework

config:
  environmentName:
    description: "Name of the environment (e.g., dev, staging, prod)"
    default: "dev"
  location:
    description: "Azure region for resources"
    default: "eastus"
  principalId:
    description: "Principal ID for role assignments"